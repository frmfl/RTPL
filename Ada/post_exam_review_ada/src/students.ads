with Review_Settings;
use Review_Settings;

package students is

   -- static task at package level. TODO: make three of them.
   task type task_stud; --is
      --entry Print ( whatever : in integer );   //ravenscar forbids entries
   --end task_stud;


end students;
